// @flow
import React from 'react';
import Button from '#atom/Button';

const HomePage = () => (
  <div>
    Home page component
    <Button>home btn text</Button>
  </div>
);

export default HomePage;
